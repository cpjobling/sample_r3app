%h1 Edit Profile

= form_for(@user) do |f|
  = render :partial => 'form', :locals => { :f => f}
  .actions
    = f.submit "Update"
  %div
    = gravatar_for @user
    = link_to "change", "http://gravatar.com/emails", :target => "_blank"